spring:
  application:
    name: gateway-service
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848
    gateway:
      routes:
        - id: service-consumer
          uri: lb://service-consumer
          predicates:
            - Path=/test/**
        #- id: order-service
        #  uri: lb://order-service
        #  predicates:
        #    - Path=/orders/**